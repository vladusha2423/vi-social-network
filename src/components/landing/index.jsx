import React from 'react';
import 'antd/dist/antd.css';
import { Layout, Menu } from 'antd';
const { Header, } = Layout;

export const Landing = () => {
    return (
        <div>

        </div>
    );
}